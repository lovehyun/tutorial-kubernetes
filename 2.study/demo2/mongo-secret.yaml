apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
type: Opaque
data:
  root-username:  cm9vdA== # base64 encoding (echo -n 'root' | base64)
  root-password:  cGFzcw== # base64 encoding (echo -n 'pass' | base64)
